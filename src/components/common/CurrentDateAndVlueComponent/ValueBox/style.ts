import { makeStyles } from '@material-ui/core';
export const useStyle = makeStyles((theme) => ({
  container: {},
  label: {
    fontWeight: 400,
    fontSize: '14px',
    lineHeight: '140%',
    color: '#6F6BC5',
    marginBottom: 8,
  },
}));
